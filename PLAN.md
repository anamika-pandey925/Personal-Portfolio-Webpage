# GSD Plan - Anamika Pandey Portfolio

<task id="init">
<action>Initialize Vite React project with Tailwind CSS</action>
<action>Install dependencies: framer-motion, three, @react-three/fiber, @react-three/drei, lucide-react, clsx, tailwind-merge</action>
<action>Set up folder structure (components, sections, hooks, assets)</action>
<action>Configure Tailwind theme and global CSS</action>
<verify>Verify project runs and styles are applied</verify>
</task>

<task id="ui">
<action>Implement UI primitives (Button, GlassCard, SectionTitle)</action>
<action>Implement Layout (Navbar, Footer)</action>
<verify>Visual check of UI elements</verify>
</task>

<task id="sections">
<action>Implement Hero section with 3D elements</action>
<action>Implement About section with counters</action>
<action>Implement Skills section with categorized cards</action>
<action>Implement Projects section with 3D tilt</action>
<action>Implement Experience timeline</action>
<action>Implement Education and Contact sections</action>
<verify>Scroll through all sections and check animations</verify>
</task>

<task id="finish">
<action>Add scroll parallax and Three.js background</action>
<action>Check mobile responsiveness</action>
<action>Run build and check for errors</action>
<verify>Final project walkthrough</verify>
</task>
